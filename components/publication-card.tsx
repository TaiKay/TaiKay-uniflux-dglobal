import Link from "next/link";
import type { Publication } from "@/lib/publications";
import { Icon } from "@/components/icon";
export function PublicationCard({ publication, featured = false }: { publication: Publication; featured?: boolean }) { return <article className={`group flex h-full flex-col justify-between border border-slate-200 p-6 transition duration-300 hover:-translate-y-1 hover:border-blue ${featured ? "bg-navy text-white md:p-9" : "bg-white"}`}><div><div className="flex items-center justify-between gap-3"><p className={`text-xs font-bold tracking-[.13em] uppercase ${featured ? "text-gold" : "text-blue"}`}>{publication.type}</p><span className={`text-xs ${featured ? "text-slate-300" : "text-slate"}`}>{publication.readingTimeMinutes} min read</span></div><p className={`mt-8 text-xs font-semibold ${featured ? "text-blue-100" : "text-slate"}`}>{publication.category}</p><h2 className="display mt-3 text-2xl font-semibold leading-tight">{publication.title}</h2><p className={`mt-4 leading-7 ${featured ? "text-slate-300" : "text-slate"}`}>{publication.summary}</p></div><Link href={`/insights/${publication.slug}`} className={`mt-8 inline-flex items-center gap-2 text-sm font-bold ${featured ? "text-white hover:text-gold" : "text-blue hover:text-navy"}`}>Read publication <Icon name="arrow" className="h-4 w-4" /></Link></article>; }
