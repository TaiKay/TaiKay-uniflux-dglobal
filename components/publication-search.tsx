"use client";
import { useMemo, useState } from "react";
import type { Publication } from "@/lib/publications";
import { PublicationCard } from "@/components/publication-card";
export function PublicationSearch({ publications }: { publications: Publication[] }) { const [query, setQuery] = useState(""); const [category, setCategory] = useState("All"); const results = useMemo(() => publications.filter((item) => (category === "All" || item.category === category) && `${item.title} ${item.summary} ${item.tags.join(" ")}`.toLowerCase().includes(query.toLowerCase())), [query, category, publications]); const categories = ["All", ...Array.from(new Set(publications.map((item) => item.category)))]; return <><div className="mt-10 flex flex-col gap-4 border-y border-slate-200 py-5 lg:flex-row lg:items-center lg:justify-between"><label className="sr-only" htmlFor="publication-search">Search publications</label><input id="publication-search" value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search publications" className="w-full border border-slate-300 bg-white px-4 py-3 text-sm outline-none placeholder:text-slate focus:border-blue lg:max-w-md"/><div className="flex flex-wrap gap-2" aria-label="Filter publications">{categories.map((item) => <button type="button" onClick={() => setCategory(item)} aria-pressed={category === item} className={`px-3 py-2 text-xs font-bold ${category === item ? "bg-navy text-white" : "border border-slate-200 text-slate hover:border-blue hover:text-blue"}`} key={item}>{item}</button>)}</div></div><p className="mt-6 text-sm text-slate" aria-live="polite">{results.length} {results.length === 1 ? "publication" : "publications"} available</p><div className="mt-6 grid gap-4 md:grid-cols-2 lg:grid-cols-3">{results.map((publication) => <PublicationCard key={publication.slug} publication={publication} />)}</div>{results.length === 0 && <p className="mt-10 border-t border-slate-200 pt-8 text-slate">No publications match that search. Try a different term or category.</p>}</>; }
