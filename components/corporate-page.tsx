import Link from "next/link";
import { Icon, type IconName } from "@/components/icon";
import { PageHero } from "@/components/page-hero";
import { Reveal } from "@/components/reveal";
type Point={title:string;description:string;icon?:IconName};
export function CorporatePage({eyebrow,title,intro,points,ctaLabel="Start a conversation",ctaHref="/contact"}:{eyebrow:string;title:string;intro:string;points:Point[];ctaLabel?:string;ctaHref?:string}){return <main><PageHero eyebrow={eyebrow} title={title} intro={intro}/><section className="container py-20 md:py-28"><div className="grid gap-8 md:grid-cols-[.75fr_1.25fr]"><p className="eyebrow">Our approach</p><Reveal><h2 className="display text-4xl font-semibold leading-tight">Built for considered, responsible progress.</h2><p className="mt-5 max-w-2xl leading-8 text-slate">Our work is shaped by the context of each organisation, the people affected by its decisions and the practical conditions required for progress to endure.</p></Reveal></div><div className="mt-14 grid gap-4 md:grid-cols-3">{points.map((point,index)=><Reveal className="border-t border-slate-200 pt-6" key={point.title}><span className="number">0{index+1}</span>{point.icon&&<Icon name={point.icon} className="mt-7 h-6 w-6 text-blue"/>}<h2 className="font-display mt-5 text-2xl font-semibold">{point.title}</h2><p className="mt-3 leading-7 text-slate">{point.description}</p></Reveal>)}</div><Link href={ctaHref} className="mt-14 inline-block bg-navy px-5 py-3 text-sm font-semibold text-white transition hover:bg-blue">{ctaLabel} →</Link></section></main>}
