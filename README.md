# UNIFLUX-D Global Limited

Corporate website foundation built with Next.js App Router, React, TypeScript and Tailwind CSS.

## Local development

```bash
npm install
npm run dev
```

The site is ready for deployment on Vercel. Before launch, update the production domain in `app/layout.tsx`, `app/sitemap.ts` and `app/robots.ts` if the final public domain differs.
